image: node:18.20.8-alpine3.21

stages:
  - build

build:
  stage: build
  script:
    - mv src public
    - npm install
    - npm run build
    - for file in $(ls); do if test "$file" != "public"; then rm -rf "$file"; done
  only:
    - online-gitlab
