{
  "engines": {
    "node": ">=18.20.8"
  },
  "scripts": {
    "build": "./build.js",
    "build-dev": "./build.js jump-compressing",
    "deploy": "npx http-server -c-1 -p 8080",
    "compress-json": "./compress-json.js"
  },
  "devDependencies": {
    "sass": "^1.90.0",
    "terser": "^5.43.1",
    "clean-css-cli": "^5.6.3",
    "http-server": "^14.1.1"
  }
}
