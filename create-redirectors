#!/usr/bin/env bash

declare -r DIR="$(dirname "$(realpath "$0")")"
declare dest

for dirname in "nest" "pages" "pages/home"
do
	dest="$DIR/$dirname"

	if [[ -d "$dest" ]]
	then
		rm -r "$dest"
	fi

	mkdir --parents "$dest"
	cp "$DIR/index.html" "$dest"
done
